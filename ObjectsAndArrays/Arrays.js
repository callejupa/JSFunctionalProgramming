//Arrays are a data structure

//They are commonly used for ordered data like lists

let person = []
person.name = 'Don Calle'

let who = person.name

console.log(who)

//Fundamentally, arrays are an object
if(typeof person === 'array'){
  console.log('Yesssss! is an array')
} else{
  console.log('Nopi!')
}